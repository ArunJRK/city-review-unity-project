using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Visible : MonoBehaviour
{
    /*Camera main;
    public GameObject g;
    // Start is called before the first frame update
    void Start()
    {
        main = Camera.main;
    }

    // Update is called once per frame
    void Update()
    {
        var col = g.GetComponentInChildren<Renderer>().material.GetColor("_TintColor");
        if (5 < main.transform.eulerAngles.x)
        {
            col.a = 1f;
        }
        else col.a = 0.3f;
    }*/
}